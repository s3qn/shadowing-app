# Where the backend lives. Locally that is http://127.0.0.1:8020/shadow, from a
# phone it is the cloudflared route.
EXPO_PUBLIC_SHADOW_API_URL=https://dev.sean.build/shadow
# Same value as SHADOW_TOKEN in backend/.env
EXPO_PUBLIC_SHADOW_TOKEN=
